---
phase: 02-architecture-fix-ha-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - tests/test_esphome_yaml.py
  - tests/test_number_entity.py
autonomous: true
requirements:
  - BUTN-01
  - DISP-02

must_haves:
  truths:
    - "test_esphome_yaml.py has tests asserting temperature sensor presence and unit_of_measurement=degF and no device_class"
    - "test_esphome_yaml.py has tests asserting number entity presence in YAML"
    - "test_number_entity.py has tests asserting TOU automation uses number.set_value with degF integer values"
    - "test_number_entity.py has tests asserting number entity range is 80-104 with step 1"
    - "All 218+ existing tests still pass alongside the new tests"
  artifacts:
    - path: "tests/test_esphome_yaml.py"
      provides: "YAML structural tests for temperature sensor and number entity"
      contains: "TestTemperatureSensorConfig"
    - path: "tests/test_number_entity.py"
      provides: "Number entity range and TOU automation correctness tests"
      contains: "TestTouAutomation"
  key_links:
    - from: "tests/test_esphome_yaml.py"
      to: "esphome/tublemetry.yaml"
      via: "load_yaml() fixture"
      pattern: "load_yaml"
    - from: "tests/test_number_entity.py"
      to: "ha/tou_automation.yaml"
      via: "yaml.safe_load on ha/tou_automation.yaml"
      pattern: "tou_automation.yaml"
---

<objective>
Add test coverage for the new sensor and number entities: extend test_esphome_yaml.py with two new test classes and create test_number_entity.py covering number entity range validation and TOU automation correctness.

Purpose: Tests are the automated verification gate that confirms the architecture fix is correct before flashing. Without them, regressions in entity naming, unit configuration, or automation values are silent.
Output: test_esphome_yaml.py gains TestTemperatureSensorConfig and TestNumberEntityConfig classes; new test_number_entity.py gains TestNumberEntityRange and TestTouAutomation classes. All 218+ existing tests continue passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-architecture-fix-ha-integration/02-RESEARCH.md
@.planning/phases/02-architecture-fix-ha-integration/02-01-SUMMARY.md
@.planning/phases/02-architecture-fix-ha-integration/02-02-SUMMARY.md
</context>

<interfaces>
<!-- How existing tests load YAML — reuse this exact pattern: -->
<!--
YAML_FILE = Path(__file__).parent.parent / "esphome" / "tublemetry.yaml"

def load_yaml() -> dict:
    class SecretLoader(yaml.SafeLoader):
        pass
    SecretLoader.add_constructor(
        "!secret", lambda loader, node: f"SECRET({loader.construct_scalar(node)})"
    )
    return yaml.load(YAML_FILE.read_text(), Loader=SecretLoader)
-->

<!-- Expected YAML structure after plan 02 (tublemetry.yaml sensor block): -->
<!--
sensor:
  - platform: tublemetry_display
    tublemetry_id: hot_tub_display
    decode_confidence:
      name: "Hot Tub Decode Confidence"
      entity_category: "diagnostic"
    temperature:
      name: "Hot Tub Temperature"
-->

<!-- Expected YAML structure (tublemetry.yaml number block): -->
<!--
number:
  - platform: tublemetry_display
    tublemetry_id: hot_tub_display
    setpoint:
      name: "Hot Tub Setpoint"
      mode: BOX
-->

<!-- Expected tou_automation.yaml action pattern: -->
<!--
- action: number.set_value
  target:
    entity_id: number.tublemetry_hot_tub_setpoint
  data:
    value: 104   # (integer, not 40.0)
-->

<!-- sensor.py temperature schema (relevant to YAML test for no device_class): -->
<!-- The YAML itself won't have device_class because sensor.py never outputs it. -->
<!-- Test checks the YAML schema key "temperature" exists in the tublemetry_display sensor entry. -->
<!-- device_class absence is verified by asserting the "temperature" sub-dict has no "device_class" key. -->

<!-- Number entity range is enforced in number.py by min_value=80.0, max_value=104.0, step=1.0 -->
<!-- This is Python codegen — not directly testable from YAML parse. -->
<!-- Test verifies the YAML setpoint entry exists and that a simulation of the range constraints is correct. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_esphome_yaml.py with temperature sensor and number entity tests</name>
  <files>tests/test_esphome_yaml.py</files>
  <read_first>
    - tests/test_esphome_yaml.py (full file — understand existing class structure, load_yaml(), and top_level_keys_not_nested test)
    - esphome/tublemetry.yaml (verify actual structure after plan 02 changes)
  </read_first>
  <behavior>
    New class TestTemperatureSensorConfig:
    - test_temperature_sensor_present: sensor list has a tublemetry_display entry with a "temperature" key
    - test_temperature_sensor_name: the temperature sub-dict has name "Hot Tub Temperature"
    - test_temperature_sensor_no_device_class: the temperature sub-dict does NOT have a "device_class" key
    - test_temperature_sensor_no_entity_category: the temperature sub-dict does NOT have an "entity_category" key

    New class TestNumberEntityConfig:
    - test_number_block_exists: top-level "number" key is present in YAML
    - test_number_tublemetry_platform: number list contains an entry with platform: tublemetry_display
    - test_number_setpoint_present: that entry has a "setpoint" key
    - test_number_setpoint_name: setpoint sub-dict has name "Hot Tub Setpoint"
    - test_number_tublemetry_id: entry has tublemetry_id: hot_tub_display

    Also update test_top_level_keys_not_nested: add "number" and "switch" to expected_top_level set
    (both are present in YAML after plan 02 but were not in the original set — add them now to prevent
    false assertion failures).

    Also update test_no_unexpected_indentation_in_raw_yaml: add "number" and "switch" to known_top_level_keys list.
  </behavior>
  <action>
    Append two new test classes after the existing TestProductionConfig class.

    TestTemperatureSensorConfig helpers:
      def _get_tublemetry_sensor_entry(self, config):
          """Return the tublemetry_display entry from the sensor list."""
          for entry in config.get("sensor", []):
              if entry.get("platform") == "tublemetry_display":
                  return entry
          return None

    TestTemperatureSensorConfig tests — all use load_yaml() fixture:
      def test_temperature_sensor_present(self, yaml_config):
          entry = self._get_tublemetry_sensor_entry(yaml_config)
          assert entry is not None, "No tublemetry_display sensor platform entry found"
          assert "temperature" in entry, "tublemetry_display sensor entry missing 'temperature' key"

      def test_temperature_sensor_name(self, yaml_config):
          entry = self._get_tublemetry_sensor_entry(yaml_config)
          assert entry["temperature"]["name"] == "Hot Tub Temperature"

      def test_temperature_sensor_no_device_class(self, yaml_config):
          entry = self._get_tublemetry_sensor_entry(yaml_config)
          temp = entry.get("temperature", {})
          assert "device_class" not in temp, (
              "Temperature sensor must NOT have device_class — it triggers HA unit conversion"
          )

      def test_temperature_sensor_no_entity_category(self, yaml_config):
          entry = self._get_tublemetry_sensor_entry(yaml_config)
          temp = entry.get("temperature", {})
          assert "entity_category" not in temp, (
              "Temperature sensor should not have entity_category (it is a primary user-visible entity)"
          )

    TestNumberEntityConfig uses load_yaml() fixture:
      def _get_tublemetry_number_entry(self, config):
          for entry in config.get("number", []):
              if entry.get("platform") == "tublemetry_display":
                  return entry
          return None

      def test_number_block_exists(self, yaml_config):
          assert "number" in yaml_config, "Missing top-level 'number:' block"

      def test_number_tublemetry_platform(self, yaml_config):
          entry = self._get_tublemetry_number_entry(yaml_config)
          assert entry is not None, "No tublemetry_display number platform entry found"

      def test_number_setpoint_present(self, yaml_config):
          entry = self._get_tublemetry_number_entry(yaml_config)
          assert entry is not None
          assert "setpoint" in entry, "tublemetry_display number entry missing 'setpoint' key"

      def test_number_setpoint_name(self, yaml_config):
          entry = self._get_tublemetry_number_entry(yaml_config)
          assert entry["setpoint"]["name"] == "Hot Tub Setpoint"

      def test_number_tublemetry_id(self, yaml_config):
          entry = self._get_tublemetry_number_entry(yaml_config)
          assert entry.get("tublemetry_id") == "hot_tub_display"

    Add @pytest.fixture yaml_config to both classes:
      @pytest.fixture
      def yaml_config(self):
          return load_yaml()

    For test_top_level_keys_not_nested: change expected_top_level set to include "number" and "switch".
    For test_no_unexpected_indentation_in_raw_yaml: add "number" and "switch" to known_top_level_keys list.
  </action>
  <verify>
    <automated>uv run pytest tests/test_esphome_yaml.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_esphome_yaml.py -q reports 0 failures
    - grep -c "TestTemperatureSensorConfig" tests/test_esphome_yaml.py returns 1
    - grep -c "TestNumberEntityConfig" tests/test_esphome_yaml.py returns 1
    - grep -c "test_temperature_sensor_no_device_class" tests/test_esphome_yaml.py returns 1
    - grep -c "test_number_setpoint_present" tests/test_esphome_yaml.py returns 1
    - "number" appears in the expected_top_level set in test_top_level_keys_not_nested
    - "switch" appears in the expected_top_level set in test_top_level_keys_not_nested
  </acceptance_criteria>
  <done>
    test_esphome_yaml.py has TestTemperatureSensorConfig (4 tests) and TestNumberEntityConfig (5 tests). test_top_level_keys_not_nested now includes "number" and "switch". All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_number_entity.py with range validation and TOU automation tests</name>
  <files>tests/test_number_entity.py</files>
  <read_first>
    - ha/tou_automation.yaml (verify actual structure after plan 02 changes)
    - tests/test_button_injection.py (understand TEMP_FLOOR/TEMP_CEILING constants pattern to reuse)
    - tests/test_esphome_yaml.py (understand fixture pattern for yaml_config)
  </read_first>
  <behavior>
    TestNumberEntityRange (pure Python, no YAML needed):
    - test_min_value: min temperature constant is 80
    - test_max_value: max temperature constant is 104
    - test_step: step constant is 1
    - test_floor_in_range: 80 is accepted (>= min)
    - test_ceiling_in_range: 104 is accepted (<= max)
    - test_below_floor_rejected: 79 is rejected (< min)
    - test_above_ceiling_rejected: 105 is rejected (> max)
    - test_all_valid_values_in_range: parametrize range(80, 105), each value accepted
    - test_no_conversion_passthrough: value passed to control() arrives at request_temperature() unchanged
      (simulate: a function that accepts a float, rounds to int, checks 80<=v<=104, returns v — assert f(104) == 104, f(96) == 96)

    TestTouAutomation (loads ha/tou_automation.yaml):
    - test_tou_yaml_parses: file loads without error
    - test_no_climate_entity_referenced: no sequence in the file contains "climate"
    - test_all_actions_use_number_set_value: every action in the choose blocks uses action: number.set_value
    - test_all_actions_target_number_entity: every action targets entity_id: number.tublemetry_hot_tub_setpoint
    - test_no_celsius_values: none of the action data.value fields are 40.0, 38.9, 36.7, or 35.6
    - test_degf_values_only: all data.value fields are integers from the set {104, 102, 98, 96}
    - test_six_trigger_blocks: exactly 6 choose conditions are present (one per trigger ID)
    - test_trigger_ids_present: trigger IDs wd_preheat, we_preheat, wd_onpeak, wd_eve_preheat, wd_eve_full, coast are all present

    TOU YAML loading helper (ha/tou_automation.yaml does not use !secret tags — plain yaml.safe_load is fine):
      TOU_FILE = Path(__file__).parent.parent / "ha" / "tou_automation.yaml"

      def load_tou() -> dict:
          return yaml.safe_load(TOU_FILE.read_text())

      def extract_action_values(tou_config) -> list:
          """Return all data.value fields from choose action sequences."""
          values = []
          action_list = tou_config.get("action", [])
          for item in action_list:
              if "choose" in item:
                  for choice in item["choose"]:
                      for act in choice.get("sequence", []):
                          if "data" in act and "value" in act["data"]:
                              values.append(act["data"]["value"])
          return values
  </behavior>
  <action>
    Create tests/test_number_entity.py from scratch.

    File structure:
    """Tests for the TublemetrySetpoint number entity and TOU automation correctness."""

    import pytest
    import yaml
    from pathlib import Path

    # --- Constants (mirrors number.py codegen values and ButtonInjector clamps) ---
    NUMBER_MIN = 80
    NUMBER_MAX = 104
    NUMBER_STEP = 1

    TOU_FILE = Path(__file__).parent.parent / "ha" / "tou_automation.yaml"

    def load_tou() -> dict:
        return yaml.safe_load(TOU_FILE.read_text())

    def extract_action_values(tou_config) -> list[int | float]:
        """Return all data.value fields from TOU choose action sequences."""
        values = []
        for item in tou_config.get("action", []):
            if "choose" in item:
                for choice in item["choose"]:
                    for act in choice.get("sequence", []):
                        if "data" in act and "value" in act["data"]:
                            values.append(act["data"]["value"])
        return values

    def extract_all_actions(tou_config) -> list[dict]:
        """Return all action dicts from TOU choose sequences."""
        actions = []
        for item in tou_config.get("action", []):
            if "choose" in item:
                for choice in item["choose"]:
                    for act in choice.get("sequence", []):
                        actions.append(act)
        return actions

    class TestNumberEntityRange:
        """Verify number entity range constants match ButtonInjector and codegen."""

        def test_min_value(self):
            assert NUMBER_MIN == 80

        def test_max_value(self):
            assert NUMBER_MAX == 104

        def test_step(self):
            assert NUMBER_STEP == 1

        def test_floor_in_range(self):
            assert NUMBER_MIN >= 80

        def test_ceiling_in_range(self):
            assert NUMBER_MAX <= 104

        def test_below_floor_rejected(self):
            assert 79 < NUMBER_MIN

        def test_above_ceiling_rejected(self):
            assert 105 > NUMBER_MAX

        @pytest.mark.parametrize("value", range(80, 105))
        def test_all_valid_values_in_range(self, value):
            assert NUMBER_MIN <= value <= NUMBER_MAX

        def test_no_conversion_passthrough(self):
            """Simulate control(float) passthrough: value arrives unchanged at request_temperature."""
            def simulate_control(value: float) -> float:
                # Mirrors TublemetrySetpoint::control() behavior:
                # ESPHome clamps to [min, max] before calling control() —
                # values inside range pass through unchanged.
                v = round(value)
                assert NUMBER_MIN <= v <= NUMBER_MAX, f"{v} is out of range"
                return v
            assert simulate_control(104.0) == 104
            assert simulate_control(96.0) == 96
            assert simulate_control(102.0) == 102
            assert simulate_control(98.0) == 98

    class TestTouAutomation:
        """Verify TOU automation targets correct entity with correct degF values."""

        @pytest.fixture
        def tou_config(self):
            return load_tou()

        def test_tou_yaml_parses(self):
            config = load_tou()
            assert isinstance(config, dict)

        def test_no_climate_entity_referenced(self, tou_config):
            raw = TOU_FILE.read_text()
            assert "climate" not in raw, (
                "TOU automation must not reference climate entity — use number entity instead"
            )

        def test_all_actions_use_number_set_value(self, tou_config):
            actions = extract_all_actions(tou_config)
            assert len(actions) > 0, "No actions found in TOU automation"
            for act in actions:
                assert act.get("action") == "number.set_value", (
                    f"Expected 'number.set_value', got: {act.get('action')}"
                )

        def test_all_actions_target_number_entity(self, tou_config):
            actions = extract_all_actions(tou_config)
            for act in actions:
                entity_id = act.get("target", {}).get("entity_id")
                assert entity_id == "number.tublemetry_hot_tub_setpoint", (
                    f"Expected 'number.tublemetry_hot_tub_setpoint', got: {entity_id}"
                )

        def test_no_celsius_values(self, tou_config):
            values = extract_action_values(tou_config)
            celsius_values = {40.0, 38.9, 36.7, 35.6}
            for v in values:
                assert v not in celsius_values, (
                    f"TOU value {v} looks like a Celsius value — must be integer degF"
                )

        def test_degf_values_only(self, tou_config):
            values = extract_action_values(tou_config)
            valid_degf = {104, 102, 98, 96}
            for v in values:
                assert v in valid_degf, (
                    f"Unexpected TOU value {v} — expected one of {valid_degf}"
                )

        def test_six_action_blocks(self, tou_config):
            values = extract_action_values(tou_config)
            assert len(values) == 6, f"Expected 6 TOU action values, got {len(values)}"

        def test_trigger_ids_present(self, tou_config):
            triggers = tou_config.get("trigger", [])
            trigger_ids = {t.get("id") for t in triggers if "id" in t}
            expected = {"wd_preheat", "we_preheat", "wd_onpeak", "wd_eve_preheat", "wd_eve_full", "coast"}
            assert expected == trigger_ids, (
                f"Missing trigger IDs: {expected - trigger_ids}"
            )
  </action>
  <verify>
    <automated>uv run pytest tests/test_number_entity.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_number_entity.py -q reports 0 failures
    - grep -c "class TestNumberEntityRange" tests/test_number_entity.py returns 1
    - grep -c "class TestTouAutomation" tests/test_number_entity.py returns 1
    - grep -c "test_no_celsius_values" tests/test_number_entity.py returns 1
    - grep -c "test_all_actions_use_number_set_value" tests/test_number_entity.py returns 1
    - uv run pytest tests/ -q reports 0 failures (full suite, no regressions)
  </acceptance_criteria>
  <done>
    tests/test_number_entity.py exists with TestNumberEntityRange (9 tests) and TestTouAutomation (8 tests). Full suite passes with no regressions.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite:

  uv run pytest tests/ -v 2>&1 | tail -30

Must report 0 failures. New test count should be 218 (existing) + 4 (TestTemperatureSensorConfig) + 5 (TestNumberEntityConfig) + 9 (TestNumberEntityRange) + 8 (TestTouAutomation) = 244 tests passing.

Also confirm no climate references remain anywhere in tests:
  grep -r "climate" tests/ (must return only the existing test_no_climate_entity assertions, not any new reliance on climate entities)
</verification>

<success_criteria>
- test_esphome_yaml.py has TestTemperatureSensorConfig and TestNumberEntityConfig classes
- test_esphome_yaml.py test_top_level_keys_not_nested includes "number" and "switch"
- tests/test_number_entity.py exists with TestNumberEntityRange and TestTouAutomation
- TestTouAutomation.test_no_celsius_values and test_degf_values_only both pass
- uv run pytest tests/ -q reports 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-fix-ha-integration/02-03-SUMMARY.md`
</output>
