---
phase: 02-architecture-fix-ha-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - esphome/tublemetry.yaml
  - ha/tou_automation.yaml
autonomous: true
requirements:
  - BUTN-01
  - DISP-02

must_haves:
  truths:
    - "tublemetry.yaml sensor block has a tublemetry_display platform entry with a temperature key"
    - "tublemetry.yaml has a top-level number: block with platform: tublemetry_display and a setpoint key"
    - "tou_automation.yaml uses action: number.set_value targeting number.tublemetry_hot_tub_setpoint"
    - "tou_automation.yaml temperature values are plain integers: 104, 104, 96, 102, 104, 98 (no Celsius)"
    - "No climate entity remains in either file"
    - "switch: top-level key is present in tublemetry.yaml (existing key must not be lost)"
  artifacts:
    - path: "esphome/tublemetry.yaml"
      provides: "Sensor + number platform entries wired to hot_tub_display"
      contains: "platform: tublemetry_display"
    - path: "ha/tou_automation.yaml"
      provides: "Corrected TOU automation with number entity + degF values"
      contains: "number.tublemetry_hot_tub_setpoint"
  key_links:
    - from: "tublemetry.yaml sensor: block"
      to: "tublemetry_display platform"
      via: "tublemetry_id: hot_tub_display"
      pattern: "tublemetry_id: hot_tub_display"
    - from: "tublemetry.yaml number: block"
      to: "tublemetry_display platform"
      via: "tublemetry_id: hot_tub_display"
      pattern: "platform: tublemetry_display"
    - from: "ha/tou_automation.yaml action"
      to: "number.tublemetry_hot_tub_setpoint"
      via: "number.set_value service call"
      pattern: "number.tublemetry_hot_tub_setpoint"
---

<objective>
Wire the sensor and number codegen into tublemetry.yaml, and fix tou_automation.yaml to target the number entity with plain degF values.

Purpose: The C++ and Python codegen from plan 01 only takes effect when tublemetry.yaml has the corresponding platform entries. The automation must be corrected before the flash or it will silently no-op against a non-existent climate entity.
Output: Updated tublemetry.yaml with sensor temperature entry and number setpoint entry; corrected tou_automation.yaml with number.set_value and integer degF values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-architecture-fix-ha-integration/02-RESEARCH.md
@.planning/phases/02-architecture-fix-ha-integration/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Current tublemetry.yaml sensor block (lines 100-113) — executor must extend, not replace: -->
<!--
sensor:
  - platform: wifi_signal
    name: "Hot Tub WiFi Signal"
    update_interval: 60s
    entity_category: "diagnostic"
  - platform: uptime
    name: "Hot Tub Uptime"
    entity_category: "diagnostic"
  - platform: tublemetry_display
    tublemetry_id: hot_tub_display
    decode_confidence:
      name: "Hot Tub Decode Confidence"
      entity_category: "diagnostic"
-->
<!-- The tublemetry_display sensor entry must gain a temperature key alongside decode_confidence. -->
<!-- Do NOT create a second tublemetry_display entry — add temperature inside the existing one. -->

<!-- ESPHome device name: "tublemetry" (from esphome.name). Entity ID derivation: -->
<!-- number entity named "Hot Tub Setpoint" -> entity_id: number.tublemetry_hot_tub_setpoint -->
<!-- sensor entity named "Hot Tub Temperature" -> entity_id: sensor.tublemetry_hot_tub_temperature -->

<!-- TOU temperature mapping (correct degF values, per D001): -->
<!-- wd_preheat:     104 -->
<!-- we_preheat:     104 -->
<!-- wd_onpeak:       96 -->
<!-- wd_eve_preheat: 102 -->
<!-- wd_eve_full:    104 -->
<!-- coast:           98 -->

<!-- tou_automation.yaml action syntax for HA number entity: -->
<!--
- action: number.set_value
  target:
    entity_id: number.tublemetry_hot_tub_setpoint
  data:
    value: 104
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add temperature sensor and setpoint number entries to tublemetry.yaml</name>
  <files>esphome/tublemetry.yaml</files>
  <read_first>
    - esphome/tublemetry.yaml (full file — understand existing sensor block structure and all top-level keys)
    - esphome/components/tublemetry_display/sensor.py (confirm CONF_TEMPERATURE key name is "temperature")
    - esphome/components/tublemetry_display/number.py (confirm CONF_SETPOINT key name is "setpoint" and CONF_TUBLEMETRY_ID is "tublemetry_id")
  </read_first>
  <action>
    Make two targeted edits to tublemetry.yaml. Do not rewrite the entire file.

    EDIT 1 — Add temperature key to the existing tublemetry_display sensor platform entry.
    Find the block:
      - platform: tublemetry_display
        tublemetry_id: hot_tub_display
        decode_confidence:
          name: "Hot Tub Decode Confidence"
          entity_category: "diagnostic"

    Change it to:
      - platform: tublemetry_display
        tublemetry_id: hot_tub_display
        decode_confidence:
          name: "Hot Tub Decode Confidence"
          entity_category: "diagnostic"
        temperature:
          name: "Hot Tub Temperature"

    No entity_category, no device_class on the temperature entry. Name "Hot Tub Temperature"
    will produce entity ID sensor.tublemetry_hot_tub_temperature.

    EDIT 2 — Add a new top-level number: block after the sensor: block (before text_sensor:).
    Insert:
      number:
        - platform: tublemetry_display
          tublemetry_id: hot_tub_display
          setpoint:
            name: "Hot Tub Setpoint"
            mode: BOX

    Name "Hot Tub Setpoint" will produce entity ID number.tublemetry_hot_tub_setpoint.

    ALSO: The test_top_level_keys_not_nested test in test_esphome_yaml.py checks for specific
    top-level keys. The "switch" key is in the file but NOT in the expected_top_level set in that
    test. Do NOT add "number" to the test (that is plan 03's job). Just add the YAML correctly.

    After editing, verify the YAML parses cleanly:
      uv run python -c "import yaml; yaml.safe_load(open('esphome/tublemetry.yaml').read())"
    (This will warn on !secret tags but must not raise an exception — use the SecretLoader pattern
    or accept the ConstructorError for !secret and just verify the structure is valid YAML.)
  </action>
  <verify>
    <automated>
      grep -n "platform: tublemetry_display" /Users/chriskaschner/Documents/GitHub/tubtron/esphome/tublemetry.yaml | wc -l
    </automated>
    Expect 2 lines (one in sensor:, one in number:).
    Also: grep -n "Hot Tub Temperature" /Users/chriskaschner/Documents/GitHub/tubtron/esphome/tublemetry.yaml (must appear)
    Also: grep -n "Hot Tub Setpoint" /Users/chriskaschner/Documents/GitHub/tubtron/esphome/tublemetry.yaml (must appear)
    Also: grep -n "mode: BOX" /Users/chriskaschner/Documents/GitHub/tubtron/esphome/tublemetry.yaml (must appear)
    Also: grep -n "climate" /Users/chriskaschner/Documents/GitHub/tubtron/esphome/tublemetry.yaml (must return nothing)
  </verify>
  <acceptance_criteria>
    - File contains exactly 2 occurrences of "platform: tublemetry_display" (sensor + number)
    - File contains "Hot Tub Temperature" with no device_class or entity_category nested under it
    - File contains top-level "number:" key with "platform: tublemetry_display" child
    - File contains "Hot Tub Setpoint" with "mode: BOX"
    - File contains no "climate" key at any level
    - All pre-existing top-level keys (esphome, esp32, wifi, api, ota, logger, sensor, text_sensor, tublemetry_display, safe_mode, button, binary_sensor, captive_portal, switch, external_components) are still present
  </acceptance_criteria>
  <done>
    tublemetry.yaml has temperature sensor and setpoint number entries, both wired to hot_tub_display. No climate entity. All existing sensors, buttons, and connectivity config preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix tou_automation.yaml — replace climate entity with number entity and degF values</name>
  <files>ha/tou_automation.yaml</files>
  <read_first>
    - ha/tou_automation.yaml (full file — understand current broken structure with climate entity and Celsius values)
  </read_first>
  <action>
    Rewrite ha/tou_automation.yaml completely. The current file has:
    - Wrong action: climate.set_temperature
    - Wrong entity_id: climate.tublemetry_hot_tub
    - Wrong temperature values (Celsius: 40.0, 35.6, 38.9, 36.7)

    Write the correct file with:
    - action: number.set_value
    - entity_id: number.tublemetry_hot_tub_setpoint
    - Plain integer degF values: 104, 96, 102, 98

    Preserve all trigger IDs, conditions, weekday filters, and schedule times exactly as they are.
    Only change the action blocks.

    The six action blocks must be:

    wd_preheat (weekday, 04:30):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 104

    we_preheat (weekend, 05:00):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 104

    wd_onpeak (weekday, 10:00):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 96

    wd_eve_preheat (weekday, 17:30):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 102

    wd_eve_full (weekday, 19:00):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 104

    coast (all days, 22:00):
      - action: number.set_value
        target:
          entity_id: number.tublemetry_hot_tub_setpoint
        data:
          value: 98

    Update the description field to remove the TODO comment and replace with:
      "Rg-2A TOU setpoint control. Weekday schedule tracks on/off-peak windows. Weekend holds 104F all day, coasts at 10pm."
  </action>
  <verify>
    <automated>
      grep -c "number.set_value" /Users/chriskaschner/Documents/GitHub/tubtron/ha/tou_automation.yaml
    </automated>
    Expect 6 (one per trigger block).
    Also: grep -c "climate" /Users/chriskaschner/Documents/GitHub/tubtron/ha/tou_automation.yaml (must return 0)
    Also: grep -c "number.tublemetry_hot_tub_setpoint" /Users/chriskaschner/Documents/GitHub/tubtron/ha/tou_automation.yaml (expect 6)
    Also: grep "temperature:" /Users/chriskaschner/Documents/GitHub/tubtron/ha/tou_automation.yaml (must return nothing — old Celsius data key gone)
    Also: grep -E "value: (104|102|98|96)" /Users/chriskaschner/Documents/GitHub/tubtron/ha/tou_automation.yaml | wc -l (expect 6)
  </verify>
  <acceptance_criteria>
    - File contains exactly 6 occurrences of "number.set_value"
    - File contains exactly 6 occurrences of "number.tublemetry_hot_tub_setpoint"
    - File contains zero occurrences of "climate"
    - File contains zero occurrences of "40.0", "35.6", "38.9", "36.7" (old Celsius values)
    - File contains the integer values 104 (x3), 96 (x1), 102 (x1), 98 (x1)
    - All six trigger IDs present: wd_preheat, we_preheat, wd_onpeak, wd_eve_preheat, wd_eve_full, coast
    - Trigger times unchanged: 04:30, 05:00, 10:00, 17:30, 19:00, 22:00
  </acceptance_criteria>
  <done>
    tou_automation.yaml uses number.set_value against number.tublemetry_hot_tub_setpoint with plain integer degF values. No climate entity reference remains.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. YAML structure check (using SecretLoader to handle !secret tags):
   uv run pytest tests/test_esphome_yaml.py -q
   All existing tests must pass. The test_no_climate_entity test already asserts climate is absent.

2. Grep checks:
   grep -c "platform: tublemetry_display" esphome/tublemetry.yaml  (expect 2)
   grep -c "number.set_value" ha/tou_automation.yaml               (expect 6)
   grep "climate" esphome/tublemetry.yaml ha/tou_automation.yaml   (must return nothing)

3. Full suite:
   uv run pytest tests/ -q  (must report 0 failures)
</verification>

<success_criteria>
- tublemetry.yaml sensor block contains "Hot Tub Temperature" entry under tublemetry_display platform
- tublemetry.yaml has top-level number: block with Hot Tub Setpoint at mode: BOX
- tou_automation.yaml has 6x number.set_value calls targeting number.tublemetry_hot_tub_setpoint
- tou_automation.yaml temperature values are 104, 104, 96, 102, 104, 98 (integer degF only)
- uv run pytest tests/ -q passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-fix-ha-integration/02-02-SUMMARY.md`
</output>
