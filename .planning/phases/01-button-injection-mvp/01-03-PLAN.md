---
phase: 01-button-injection-mvp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_esphome_yaml.py
  - 485/scripts/ladder_capture.py
  - tests/test_ladder_capture.py
autonomous: true
requirements: [DISP-01, DISP-02]
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md reflects actual state: DISP-01 and DISP-02 show hardware verification pending, not prematurely complete"
    - "A structural YAML test catches top-level keys accidentally nested under other keys (indentation-as-nesting)"
    - "A temperature ladder capture script exists and is tested, ready to run when hardware arrives"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement status reflecting hardware-blocked state"
      contains: "In Progress"
    - path: "tests/test_esphome_yaml.py"
      provides: "Structural YAML indentation validation test"
      contains: "test_top_level_keys_not_nested"
    - path: "485/scripts/ladder_capture.py"
      provides: "Structured temperature ladder capture script for resolving 7-segment encoding ambiguity"
      min_lines: 80
    - path: "tests/test_ladder_capture.py"
      provides: "Tests for ladder capture output parsing and validation logic"
      min_lines: 40
  key_links:
    - from: "tests/test_esphome_yaml.py"
      to: "esphome/tubtron.yaml"
      via: "load_yaml() reads and validates structure"
      pattern: "test_top_level_keys_not_nested"
    - from: "tests/test_ladder_capture.py"
      to: "485/scripts/ladder_capture.py"
      via: "imports parsing/validation functions"
      pattern: "from.*ladder_capture import"
---

<objective>
Close verification gaps from 01-VERIFICATION.md: fix premature requirement status, add structural YAML test to prevent indentation-as-nesting regressions, and prepare temperature ladder capture tooling so everything is ready when hardware arrives.

Purpose: The verification found REQUIREMENTS.md prematurely marks DISP-01/DISP-02 as complete, the existing YAML tests miss structural issues (key nesting), and no structured capture tooling exists for the critical ladder capture session. These are all code-actionable fixes that do not require hardware.

Output: Updated REQUIREMENTS.md, enhanced YAML tests, tested ladder capture script.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-button-injection-mvp/01-VERIFICATION.md
@.planning/phases/01-button-injection-mvp/01-CONTEXT.md

@tests/test_esphome_yaml.py
@esphome/tubtron.yaml
@485/scripts/rs485_capture.py
@src/tubtron/decode.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/tubtron/decode.py:
```python
SEVEN_SEG_TABLE: dict[int, str]  # masked 7-bit value -> character
_CONFIRMED_KEYS = frozenset([0x30, 0x70, 0x00])

def decode_7seg(byte_val: int) -> tuple[str, str]:
    """Returns (character, confidence) where confidence is 'confirmed' or 'unverified'."""
```

From tests/test_esphome_yaml.py:
```python
def load_yaml() -> dict:
    """Load tubtron.yaml, handling ESPHome !secret tags."""
```

From 485/scripts/rs485_capture.py:
```python
# Interactive capture with serial.Serial, markers, pattern tracking
# Outputs: timestamped hex log, unique pattern summary
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md status and add structural YAML indentation test</name>
  <files>.planning/REQUIREMENTS.md, tests/test_esphome_yaml.py</files>
  <action>
Two changes in this task:

1. Update .planning/REQUIREMENTS.md:
   - Change DISP-01 checkbox from `[x]` to `[ ]` and status from "Complete" to "In Progress -- firmware ready, hardware verification pending, lookup table unverified (17/20 entries)"
   - Change DISP-02 checkbox from `[x]` to `[ ]` and status from "Complete" to "In Progress -- climate entity defined, hardware integration pending"
   - Update the traceability table at bottom to match (Status column: "Complete" -> "In Progress")
   - Add a note: "Last updated: {date} -- statuses revised per 01-VERIFICATION.md gap analysis"

2. Add a structural YAML indentation test to tests/test_esphome_yaml.py:
   - Add test `test_top_level_keys_not_nested` that verifies critical ESPHome keys (esphome, esp32, wifi, api, ota, logger, uart, external_components, climate, sensor, text_sensor) are all at the top level of the parsed YAML dict, NOT nested as children of other keys.
   - The test should recursively check all top-level values: if any value is a dict, verify none of the expected top-level keys appear as children of that dict. This catches the pattern where a leading space on `ota:` makes it a child of `api:`.
   - Add test `test_no_unexpected_indentation_in_raw_yaml` that reads the raw YAML file line by line and checks that all known top-level keys (ota, wifi, api, logger, etc.) start at column 0 (no leading whitespace) when they appear as key definitions (line matches `^keyname:`). This catches the indentation issue at the text level before YAML parsing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_esphome_yaml.py -v -x</automated>
  </verify>
  <done>
    - REQUIREMENTS.md shows DISP-01 and DISP-02 as "In Progress" with accurate descriptions of what is pending
    - Two new YAML tests exist and pass, verifying top-level keys are not accidentally nested
    - All existing YAML tests still pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Temperature ladder capture script with output parsing and tests</name>
  <files>485/scripts/ladder_capture.py, tests/test_ladder_capture.py</files>
  <behavior>
    - Test 1: parse_capture_line extracts timestamp, byte count, and hex bytes from a formatted capture line like "[   1.234] (  8 bytes) FE 06 70 E6 00 06 70 00"
    - Test 2: extract_stable_frames filters a list of parsed frames to find repeated identical patterns (stability detection -- 3+ consecutive identical frames = stable)
    - Test 3: build_ladder_entry creates a structured dict {temperature: int, stable_frames: list[bytes], raw_hex: str, byte_3_value: int} from a stable capture at a known temperature
    - Test 4: validate_ladder checks that a completed ladder has entries at 5+ distinct temperatures and covers at least 5 unique byte_3 values
    - Test 5: generate_lookup_update takes a validated ladder and produces a dict mapping byte values to confirmed digit characters, cross-referenced against SEVEN_SEG_TABLE
    - Test 6: write_ladder_csv writes ladder entries to CSV format with columns: temperature, byte_0 through byte_7, stable_frame_count, timestamp
  </behavior>
  <action>
Create a temperature ladder capture script and supporting test module.

The script (485/scripts/ladder_capture.py) should:
- Be importable as a module (functions usable from tests) AND runnable as a CLI script
- Provide functions: parse_capture_line(), extract_stable_frames(), build_ladder_entry(), validate_ladder(), generate_lookup_update(), write_ladder_csv()
- When run as CLI: guide the user through a structured temperature sweep (104 -> 80F, one step at a time)
- Use pyserial to read from a configurable serial port (default /dev/ttyUSB0 for Linux, auto-detect on macOS)
- At each temperature step: capture frames for 3 seconds, detect stability (3+ consecutive identical 8-byte frames), record the stable frame pattern
- Prompt user to confirm displayed temperature matches the physical panel, then press Temp Down
- After completing the sweep, call validate_ladder() and generate_lookup_update()
- Output: CSV file (485/captures/ladder_YYYYMMDD.csv) and a JSON summary with the byte-to-digit mapping
- Print a summary showing which SEVEN_SEG_TABLE entries are now confirmed vs still unverified

The test module (tests/test_ladder_capture.py) should:
- Import the parsing/validation functions from ladder_capture (not the serial I/O)
- Use synthetic test data (no hardware needed)
- Test all 6 behaviors listed above
- Use pytest fixtures for sample capture lines and ladder entries

Important: The CLI portion that does serial I/O is NOT testable without hardware. Structure the code so all logic is in pure functions that the tests exercise, and the CLI main() is a thin wrapper around those functions.

Port detection: use a --port CLI argument with a sensible default. Do NOT auto-detect in the importable functions -- only in main().
  </action>
  <verify>
    <automated>uv run pytest tests/test_ladder_capture.py -v -x</automated>
  </verify>
  <done>
    - ladder_capture.py exists with all 6 pure functions tested
    - All tests pass with synthetic data (no serial port needed)
    - Script is runnable via `uv run python 485/scripts/ladder_capture.py --help` (shows usage)
    - validate_ladder rejects ladders with fewer than 5 temperatures
    - generate_lookup_update correctly maps byte values to digit characters
  </done>
</task>

</tasks>

<verification>
```bash
# All tests pass (existing + new)
uv run pytest tests/ -v -x

# Ladder capture script shows help without error
uv run python 485/scripts/ladder_capture.py --help

# REQUIREMENTS.md no longer shows DISP-01/DISP-02 as Complete
grep -c "Complete" .planning/REQUIREMENTS.md | head -1
# Should return 0 for DISP-01 and DISP-02 lines
grep "DISP-0" .planning/REQUIREMENTS.md | grep -v "Complete"
```
</verification>

<success_criteria>
- All existing 56 tests still pass (no regressions)
- New YAML structural tests pass and would catch indentation-as-nesting issues
- New ladder capture tests pass with synthetic data
- REQUIREMENTS.md accurately reflects hardware-blocked state
- Ladder capture script is ready to run when hardware arrives
</success_criteria>

<output>
After completion, create `.planning/phases/01-button-injection-mvp/01-03-SUMMARY.md`
</output>
