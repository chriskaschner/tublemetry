---
phase: 01-button-injection-mvp
task: complete
total_tasks: 3
status: hardware_blocked
last_updated: 2026-03-17T21:53:30Z
---

<current_state>
Phase 1 code fully complete (3/3 plans). One verification gap remains: temperature
ladder capture at 5+ known temperatures. This session made first live attempt at
the tub but got no data -- wiring issue diagnosed, left mid-debug.

Last state: user rewiring to differential (Pin 5 → A, Pin 4 → B, no jumper).
Not yet confirmed working when session paused.
</current_state>

<completed_work>

- Plan 01-01: Python decode library (7-segment decoder, frame parser, display state machine -- 56 tests)
- Plan 01-02: ESPHome external component (C++ port, dual UART, climate entity, diagnostic sensors, YAML config)
- Plan 01-03: Gap closure (requirement status fixes, structural YAML tests, ladder capture script -- 77 tests total)
- Verification: 2/3 success criteria pass; SC1 (ladder capture) hardware-blocked
- OEM panel cable pinout documented in reference/status_03_08.md
- Waveshare adapter confirmed working on macOS: /dev/cu.usbserial-BG01XA76 (no driver needed)
- Stub cable approach finalized: cut pins 1/2/3/7/8 flush at T-splitter end
- This session: first live capture attempt at tub, port opens fine, 0 bytes received
</completed_work>

<remaining_work>

- Confirm differential wiring works (Pin 5 Blue/White → A, Pin 4 Blue → B, no GND jumper)
- Run ladder capture:
  uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-BG01XA76
- After capture: update _CONFIRMED_KEYS in src/tubtron/decode.py and C++ table in tubtron_display.cpp
- Code (can start now): /gsd:plan-phase 2 for button injection
</remaining_work>

<decisions_made>

- Waveshare adapter port: /dev/cu.usbserial-BG01XA76 (no driver install needed on this Mac)
- Stub cable: cut pins 1/2/3/7/8 conductors flush at T-splitter end
- OEM panel cable colors: Pin 1 Brown, Pin 2 Blue, Pin 3 Yellow, Pin 4 Green, Pin 5 Red, Pin 6 Black, Pin 7 Orange, Pin 8 Grey
- Single-ended wiring (Pin 5 → A, Pin 4 → GND) did NOT work -- 0 bytes at any baud rate
- B-GND jumper also did NOT work
- Correct wiring: DIFFERENTIAL -- Pin 5 (Blue/White) → A, Pin 4 (Blue) → B (they are a twisted pair)
- No GND connection needed on Waveshare for differential capture
</decisions_made>

<blockers>

- Temperature ladder capture: wiring not yet confirmed working
  Last attempted: differential (Pin 5 → A, Pin 4 → B), result unknown when session paused
- AliExpress parts (ESP32, photorelays, RJ45 screw terminal breakouts, resistors, DC-DC): in transit
</blockers>

<context>
Correct wiring for differential RS-485 capture of Pin 5 stream:
  RJ45 pin 5 → Blue/White → Waveshare A
  RJ45 pin 4 → Blue       → Waveshare B
  (no GND wire needed)

Pin 5 (Blue/White) and Pin 4 (Blue) are the T568B Blue twisted pair -- they
are the differential A/B lines for the Pin 5 RS-485 channel. GND on the
Waveshare adapter can float.

Quick raw test before running ladder_capture.py:
  uv run python -c "
  import serial, time
  ser = serial.Serial('/dev/cu.usbserial-BG01XA76', 115200, timeout=0.5)
  ser.reset_input_buffer()
  start = time.time()
  while time.time() - start < 5:
      data = ser.read(256)
      if data:
          print(f'{len(data)} bytes: {data.hex()}')
  ser.close()
  "

After capture, update two files:
  - src/tubtron/decode.py: _CONFIRMED_KEYS dict and SEVEN_SEG_TABLE entries
  - esphome/components/tubtron_display/tubtron_display.cpp: C++ table comments
Then re-run verification to close the final gap.
</context>

<next_action>
Start with: confirm differential wiring (Pin 5 → A, Pin 4 → B) gives data using quick raw test above.
If working, run: uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-BG01XA76 --flash
</next_action>
