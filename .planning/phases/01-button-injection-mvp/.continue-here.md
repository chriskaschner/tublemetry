---
phase: 01-button-injection-mvp
status: complete — hardware wiring session done, system live
last_updated: 2026-03-31T02:00:16.767Z
---

<current_state>
Phase 01 code complete and verified. Session 2026-03-30 was hardware bring-up:
wiring ESP32 to RJ45, powering from board, TOU schedule live in HA.
System running autonomously. Tub at 99F heating to 104F setpoint at end of session.
</current_state>

<completed_work>

Previous sessions (see prior history above for detail):
- Full firmware: display decode, button injection, probe+cache, climate entity
- All 218 tests passing, OTA flashing via WiFi confirmed

Session 2026-03-30:
- ESP32 powered directly from RJ45 Pin 1 (+5V) with 540µF bulk caps (2x220 + 1x100)
- B0505S-1W isolated converter attempted but DOA (clone) — direct wiring used instead
- WiFi power_save_mode: LIGHT, logger: INFO
- Relay hold switches added (optimistic template GPIO18/19) for bench testing
- TOU schedule automation loaded into HA (ha/tou_automation.yaml) — ACTIVE
- Heating rate tracker automations created (ha/heating_tracker.yaml) — INSTALLED
- Heating analysis script created (scripts/analyze_heating.py)
- WIRING-GUIDE.md and README fully updated
- Committed and pushed: 7f2f3b4
</completed_work>

<remaining_work>

- Create 5 HA input_number helpers for heating tracker (Settings → Helpers)
- Test heating tracker automation fires correctly on next setpoint change
- Relay hold switches not yet confirmed working — verify or remove
- B0505S-1W replacement: get working Mornsun unit for cleaner power isolation
- Deep sleep / periodic wake (deferred — WiFi LIGHT sufficient for now)
</remaining_work>

<decisions_made>

- Skip DOA B0505S-1W clone — direct Pin 1 to ESP32 VIN with bulk caps
- 540µF = 2x220µF + 1x100µF in parallel across VIN/GND
- WiFi LIGHT power save sufficient; deep sleep deferred
- Photorelay commons on Pin 1 directly (isolation moot with shared GND)
- Heating tracker uses HA recorder + input_number helpers for queryable time-series
- Analysis script targets HA SQLite DB directly (home-assistant_v2.db)
- probe_once() invalidates known_setpoint_ (from prior session)
- GPIO18=temp_down, GPIO19=temp_up
</decisions_made>

<blockers>

- None blocking. HA helpers for heating tracker not yet created (manual UI step).
</blockers>

<context>
System is live and self-managing via TOU schedule. Weekend schedule active:
05:00 → 104F, 22:00 → 98F. Heating tracker will accumulate data automatically.
After a few weeks of data, run scripts/analyze_heating.py to model heat-up time
vs outdoor temp for smarter preheat lead times.
</context>

<next_action>
1. Create 5 HA helpers for heating tracker (see ha/heating_tracker.yaml header for names/types)
2. Manually run "Hot Tub Log Heating Command" automation to verify helpers update
3. Check helpers in Developer Tools → States (search hot_tub_last)
</next_action>
