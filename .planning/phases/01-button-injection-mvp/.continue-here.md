---
phase: 01-button-injection-mvp
task: firmware-fixed
total_tasks: 8
status: probe-test-pending
last_updated: 2026-03-27T12:45:50.135Z
---

<current_state>
known_setpoint invalidation bug fixed and flashed. Device at 192.168.0.92, water temp
~103F, tub setpoint ~94F (left from earlier failed sequence). Fixed firmware is live.
Next: trigger climate entity in HA to test the probe sequence end-to-end.

The probe should: fire one down press -> read setpoint flash (~93-94F) -> count up
10 presses to 104F. Verification may time out (known limitation) but setpoint should land.
</current_state>

<completed_work>

- Plan 01-01: Python decode library (7-segment decoder, frame parser, display state machine)
- Plan 01-02: ESPHome external component (GPIO interrupt, clock+data)
- Plan 01-03: Gap closure (requirement status fixes, structural YAML tests, ladder capture)
- Ladder capture (2026-03-20): all digits 0-9 confirmed, 0x73="9" (differs from GS510SZ 0x7B)
- Mode display captures: 0x4F="E", 0x0D="c", 0x0E="L", 0x0F="t"
- 0x34="1" added (setpoint display mode)
- ESPHome firmware: GPIO interrupt, pulldown on clock/data pins
- Button injection hardware validated both directions
- 218 tests passing

Session 2026-03-27:
- Root-caused first probe failure: known_setpoint was stale (102F from prior success) after
  test button presses changed real setpoint to ~92F. Firmware did 2 up-presses (102->104)
  but actual setpoint only moved 92->94. Verification correctly timed out.
- After timeout, firmware fired more relays -> ISR noise -> WiFi crash (device offline ~20min)
- Fix: press_once() now invalidates known_setpoint_ (NAN) so next sequence probes
- Fix: timeout/abort also invalidate known_setpoint_ (uncertain where setpoint landed)
- 6 new tests added, existing wrong-behavior test corrected
- Decided: probe+cache over always-rehome (user preference -- fewer presses)
- OTA flashed successfully
</completed_work>

<remaining_work>

- Test probe sequence end-to-end: set 104F in HA, watch probe find ~94F, 10 up-presses
- Wire TOU automation in HA (weekday schedule confirmed; weekend schedule TBD)
- Setpoint-display vs current-temp disambiguation (non-blocking)
</remaining_work>

<decisions_made>

- press_once() invalidates known_setpoint_ -- test presses change real hardware setpoint
- timeout/abort invalidate known_setpoint_ -- don't know where setpoint landed after failure
- probe+cache strategy over always-rehome -- user preference: 10 presses (94->104) vs 49 (25 down + 24 up)
- GPIO18=temp_down, GPIO19=temp_up (confirmed via test, swapped from original assumption)
- 0x34="1" in setpoint display mode
- Protocol is synchronous clock+data (SPI-like), NOT RS-485 UART
- 0x73 = "9" on VS300FL4 (NOT 0x7B as GS510SZ)
</decisions_made>

<known_issues>

- Setpoint display frames (~1s after button press) may be decoded as current temp
  -- no way to distinguish from raw 7-seg data alone
  -- verification timeout is expected; setpoint is still set correctly on the tub
- ISR can flood WiFi stack if clock pin gets electrical noise during relay firing
  -- pulldown resistors help; serial flash required if OTA becomes unreliable
  -- post-timeout recovery was also firing relays (now fixed -- finish_sequence_ clears pins first)
</known_issues>

<context>
Session 2026-03-27: ran first real climate entity sequence (target 104F). Firmware took the
"known setpoint" shortcut (102F cached from prior success) but real setpoint was ~92F from
test presses. Probe+cache architecture was already correct but press_once() wasn't invalidating
the cache. Fixed. Also fixed timeout/abort paths. OTA flashed.

TOU automation design is ready to implement once probe test confirms sequences work reliably.
Weekday schedule: 04:30->104F, 10:00->96F, 17:30->102F, 19:00->104F, 22:00->98F.
Weekend schedule and Siri low-temp shortcut value still needed from user.
</context>

<next_action>
1. cd esphome && PYTHONUNBUFFERED=1 uvx esphome logs tublemetry.yaml > /tmp/tub_probe_test.txt 2>&1 &
2. In HA: set Hot Tub climate to Heat, target 104F
3. After ~30s: grep -v "Partial frame\|\[C\]\|\[V\]\|text_sensor" /tmp/tub_probe_test.txt | tail -40
4. Expect: PROBING -> probe press fires -> setpoint flash read (~93-94F) -> ADJUSTING (10 up) -> VERIFYING
5. If verified: implement TOU automation YAML
6. If timeout (expected): confirm tub setpoint actually at 104F (feel water or watch display settle)
</next_action>
