---
phase: 01-button-injection-mvp
task: complete
total_tasks: 3
status: hardware_blocked
last_updated: 2026-03-14T03:41:35Z
---

<current_state>
Phase 1 code is fully complete (3/3 plans). One verification gap remains: temperature
ladder capture at 5+ known temperatures -- hardware-blocked until the Waveshare USB
RS-485 adapter is connected to the tub. All tooling is built and tested.

This session: executed plan 01-03 (gap closure), ran verification (gaps_found),
documented OEM cable pinout, and planned the physical RS-485 tap setup.
</current_state>

<completed_work>

- Plan 01-01: Python decode library (7-segment decoder, frame parser, display state machine, TDD -- 56 tests)
- Plan 01-02: ESPHome external component (C++ port, dual UART, climate entity, diagnostic sensors, YAML config)
- Plan 01-03: Gap closure (requirement status fixes, structural YAML tests, ladder capture script -- 77 tests total)
- Verification run: 2/3 success criteria pass; SC1 (temperature ladder capture) hardware-blocked
- OEM panel cable pinout documented in reference/status_03_08.md
- RS-485 tap approach decided: custom 3-wire stub (pins 4/5/6 only) to avoid phantom presses on button lines
</completed_work>

<remaining_work>

- Hardware task: temperature ladder capture (resolves remaining verification gap)
  - Use Waveshare USB RS-485 adapter (already have it) -- NOT the ESP32
  - Build custom stub: 3 wires only, T568B cable, pins 4 (Blue/GND), 5 (Blue/White/RS-485 A), 6 (Green/RS-485 B)
  - Keep stub under 6 inches, leave pins 1/2/3/7/8 completely unwired
  - Plug into T-splitter at J1
  - Run: uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-XXXX
  - After capture: update _CONFIRMED_KEYS in decode.py and the C++ table comments in tubtron_display.cpp
- Code task: /gsd:plan-phase 2 (button injection) -- can start before hardware arrives
</remaining_work>

<decisions_made>

- Use Waveshare USB RS-485 adapter for ladder capture (not ESP32) -- direct serial port, works with ladder_capture.py immediately, no firmware needed
- Phantom presses root cause identified: T-splitter creates stubs on ALL 8 pins; unterminated >12" stub on Orange pair (Pin 1: +5V / Pin 2: Temp Up) acts as antenna
- Fix: custom stub cable with conductors ONLY at pins 4/5/6 -- button line pins have zero stub at T-splitter third port
- OEM panel cable (factory-installed) uses non-standard colors but identical pin orientation to T568B:
  Pin 1 Brown, Pin 2 Blue, Pin 3 Yellow, Pin 4 Green, Pin 5 Red, Pin 6 Black, Pin 7 Orange, Pin 8 Grey
- ESP32 + MAX485 reserved for ESPHome deployment (Phase 1 final hardware), not for capture
</decisions_made>

<blockers>

- Temperature ladder capture: requires physical tub access + Waveshare USB RS-485 adapter connected
  - Tooling is complete: 485/scripts/ladder_capture.py, 19 tests, CLI ready
  - Run when ready: uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-XXXX
- AliExpress parts (ESP32, photorelays, RJ45 screw terminal breakouts, resistors, DC-DC): in transit
</blockers>

<context>
Phase 1 is code-complete and verified except for the hardware-gated ladder capture.
The decode library (Python + C++) uses a tentative 7-segment table based on the
GS510SZ reference (0x30="1", 0x70="7" confirmed, rest unverified). Ladder capture
will confirm or correct the remaining entries.

The safe tap method: custom stub, 3 wires, pins 4/5/6 only, plugged into the T-splitter
at J1. The existing Waveshare USB RS-485 adapter connects to the stub's Wago terminals.
The panel cable and board are completely undisturbed.

After capture, update two files:
  - src/tubtron/decode.py: update _CONFIRMED_KEYS dict and SEVEN_SEG_TABLE entries
  - esphome/components/tubtron_display/tubtron_display.cpp: update the C++ table comments

Then re-run verification: it should flip SC1 to passed and phase status to passed.
</context>

<next_action>
Immediate (at tub): Build the 3-wire stub cable (pins 4/5/6 only, T568B, under 6"),
plug into T-splitter, connect Waveshare adapter, run ladder_capture.py.

If not at tub yet: /gsd:plan-phase 2 to start planning button injection while waiting.
</next_action>
