---
phase: 01-button-injection-mvp
task: complete
total_tasks: 3
status: hardware_blocked
last_updated: 2026-03-16T16:55:51Z
---

<current_state>
Phase 1 code fully complete (3/3 plans). One verification gap remains: temperature
ladder capture at 5+ known temperatures -- hardware-blocked until Waveshare USB RS-485
adapter is connected to the tub. All tooling built and tested.

This session: confirmed Waveshare adapter recognized on macOS (no driver needed),
finalized stub cable approach (cut pins 1/2/3/7/8 flush at T-splitter end, leave
no conductor in those positions).
</current_state>

<completed_work>

- Plan 01-01: Python decode library (7-segment decoder, frame parser, display state machine -- 56 tests)
- Plan 01-02: ESPHome external component (C++ port, dual UART, climate entity, diagnostic sensors, YAML config)
- Plan 01-03: Gap closure (requirement status fixes, structural YAML tests, ladder capture script -- 77 tests total)
- Verification: 2/3 success criteria pass; SC1 (ladder capture) hardware-blocked
- OEM panel cable pinout documented in reference/status_03_08.md
- Waveshare adapter confirmed working on macOS: /dev/cu.usbserial-BG01XA76 (no CH341 driver needed)
- Stub cable approach finalized: cut pins 1/2/3/7/8 flush at T-splitter end
</completed_work>

<remaining_work>

- Build stub cable: T568B, crimp RJ45 with ONLY pins 4 (Blue/GND), 5 (Blue/White/RS-485 A), 6 (Green/RS-485 B)
  Cut all other conductors flush at the T-splitter end. Keep stub under 6".
- Connect: stub into T-splitter at J1, Waveshare A/B/GND to stub Wagos
- Run ladder capture at tub:
  uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-BG01XA76
- After capture: update _CONFIRMED_KEYS in src/tubtron/decode.py and C++ table in tubtron_display.cpp
- Code (can start now): /gsd:plan-phase 2 for button injection
</remaining_work>

<decisions_made>

- Waveshare adapter port: /dev/cu.usbserial-BG01XA76 (no driver install needed on this Mac)
- Stub cable: cut pins 1/2/3/7/8 conductors flush at T-splitter end -- antenna issue, not a termination issue
- Resistor on unused pins does NOT fix phantom presses (conductor inside jacket is still the antenna)
- OEM panel cable colors: Pin 1 Brown, Pin 2 Blue, Pin 3 Yellow, Pin 4 Green, Pin 5 Red, Pin 6 Black, Pin 7 Orange, Pin 8 Grey
</decisions_made>

<blockers>

- Temperature ladder capture: requires physical tub access to run
  Tooling complete, port confirmed, stub cable construction pending
- AliExpress parts (ESP32, photorelays, RJ45 screw terminal breakouts, resistors, DC-DC): in transit
</blockers>

<context>
Stub pinout for reference:
  RJ45 pin 4 → Blue       → GND        → Waveshare GND
  RJ45 pin 5 → Blue/White → RS-485 A   → Waveshare A
  RJ45 pin 6 → Green      → RS-485 B   → Waveshare B

After capture, update two files:
  - src/tubtron/decode.py: _CONFIRMED_KEYS dict and SEVEN_SEG_TABLE entries
  - esphome/components/tubtron_display/tubtron_display.cpp: C++ table comments
Then re-run verification to close the final gap.
</context>

<next_action>
Start with: build the 3-wire stub cable, then do the ladder capture at the tub.
Command ready: uv run python 485/scripts/ladder_capture.py --port /dev/cu.usbserial-BG01XA76

Alternative if not at tub: /gsd:plan-phase 2
</next_action>
