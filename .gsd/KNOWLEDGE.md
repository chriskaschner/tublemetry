- GS510SZ reference encoding used as base for unverified 7-segment mappings (bit6=a through bit0=g)
- Dp bit (bit 7) masked before lookup -- 0x30 and 0xB0 decode to same character
- DisplayState uses pure-function update pattern returning new state objects
- Temperature outside 80-120F flagged as low confidence rather than rejected
- TubtronDisplay stores two UARTComponent pointers rather than inheriting from UARTDevice (dual UART)
- TubtronClimate is a separate class; parent TubtronDisplay holds climate pointer
- Frame boundary detection uses millis() gap > 1ms (not micros())
- SEVEN_SEG_TABLE formatted with markers for cross-check test parseability
- Pin 6 data read and discarded to prevent UART buffer overflow
- Timestamp sensor uses millis uptime instead of SNTP (simpler, no RealTimeClock dependency)
- climate.climate_schema() used instead of deprecated CLIMATE_SCHEMA
- Top-level esp32: block used instead of deprecated platform/board in esphome: block
- OTA indentation was already correct in tubtron.yaml (no fix needed -- verification report was based on stale data)
- Ladder capture byte_3_value extracts byte at index 3 (tens digit for 3-digit temps); ones digit derived from temperature % 10 in generate_lookup_update
- pytest pythonpath config added to pyproject.toml for importing 485/scripts modules
- HiLetGo ESP-32S (ESP32-D0WDQ6 rev1) WiFi radio dead on first board -- zero networks in scan despite full erase + reflash + bare-bones firmware
- esptool baud 460800 fails on CP2102 USB-serial; use 115200
- After erase_flash, must write all 4 partitions explicitly (bootloader @ 0x1000, partitions @ 0x8000, boot_app0 @ 0xe000, firmware @ 0x10000) or board boot-loops with RTCWDT_RTC_RESET
- esp-idf framework causes boot loop on this ESP32-D0WDQ6 rev1 board; Arduino framework works
- ISR attach_interrupt in ESPHome takes instance pointer argument (not void callback) -- signature: attach_interrupt(callback, instance, mode)
- Floating GPIO pins without pull-down fire ISR at noise rate; always enable internal pull-down on clock/data inputs
- MIN_PULSE_US debounce in ISR rejects edges <10µs apart (real clock pulses ~37µs)
- safe_mode component auto-included by OTA since ESPHome 2024.6.0; explicit config only needed for custom settings
- UniFi SSID "iota" broadcasts with trailing space in beacon ("iota "); ESPHome scan filter rejects on exact mismatch — secrets.yaml must match the actual beacon SSID exactly
- New ESP32 board (ESP32-D0WD-V3 rev3.1, CH340 USB-serial) works at 460800 baud unlike old HiLetGo (CP2102, 115200 only)
- Verbose logging required to see scan results — DEBUG level only shows "No networks found" without listing what was scanned
- ESP32 WiFi connected at 192.168.0.92 (tublemetry.local), signal -18 to -23 dBm
- ESPHome compilation in worktrees requires: (1) symlink secrets.yaml from project root into worktree esphome/ directory; (2) invoke venv binary directly (/path/.venv/bin/esphome compile ...) — `uv run esphome` cannot resolve esphome in worktree environments
- Checksum gate in process_frame_(): p1 (hundreds digit byte, bits 23-17) must have bits 6,3,1,0 clear (CHECKSUM_MASK=0x4B); p4 (status bits 2-0) must have bit 0 clear. Hundreds digit for valid temperatures is blank (0x00) or '1' (0x30/0x34) — both clear all mask bits. Display values starting with '0' (0x7E) are structurally invalid and caught by the gate.
- Scoped block `{ uint8_t p1 = ...; }` pattern used in process_frame_() for checksum gate to avoid variable name collisions in the larger function — good pattern for adding isolated logic blocks to existing C++ functions
- Set-mode detection: VS300FL4 alternates blank frames with setpoint-temp frames during set mode. Pattern: blank (enter set mode) → temp (candidate) → blank (confirm + publish) → temp → blank → ... Timeout at 2000ms exits set mode if no blank arrives.
- display_state_sensor_ always reflects the actual display state string ("temperature", "blank", etc.) even when temperature_sensor_ publish is suppressed — the suppression guard wraps only publish_state(), not the state variable or state-sensor update
- Python mirror tests for C++ state machines: use a plain Python class with now_ms injection parameter — no mocking needed, deterministic timing, tests run fast with zero hardware dependencies. SetModeStateMachine in tests/test_setpoint_detection.py is the reference pattern.
- worktree .venv does not exist — venv lives at project root (/Users/chriskaschner/Documents/GitHub/tubtron/.venv/bin/esphome). Always use full absolute path when compiling from worktree context.
- Status bit extraction in process_frame_(): p1_full must be re-extracted from frame_bits outside the checksum scoped block — the scoped `p1` variable is out of scope after the block closes. Pattern: `uint8_t p1_full = (frame_bits >> 17) & 0x7F;` after the scoped block.
- Binary sensor last-state tracking: initialize int8_t last_heater_/last_pump_/last_light_ to -1 (not 0) so the first frame always triggers publish regardless of whether the first real value is true or false.
- ESPHome binary_sensor.py test imports: use `from tests.test_mock_frames import` (not bare `from test_mock_frames import`) — tests/ is not in pythonpath, only 485/scripts is. Bare import raises ModuleNotFoundError.
