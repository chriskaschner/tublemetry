# heating_tracker.yaml -- DEPRECATED
#
# Superseded by thermal_model.yaml which provides SQL-based heating/cooling
# rate sensors with the correct entity references (sensor/number instead of
# the removed climate.hot_tub entity).
#
# This file is retained for reference only. It is not loaded as an HA package.
# The two automations (Log Heating Command, Log Heating Complete) used
# input_number helpers that are not defined elsewhere and referenced the
# removed climate entity.
